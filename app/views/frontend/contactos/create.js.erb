<%  if @contacto.errors.empty? %>
$('.form-contacto').collapse('hide');
$('.mensaje-contacto').collapse('show');

<% else %>

PgRails.showToast("<%= @contacto.errors.full_messages.join(", ") %>", 'Hubo un error');
<% end %>

<% if @captcha_success == 'false' %>
PgRails.showToast('Hubo un error en el envío de mensaje. Intente nuevamente, por favor.');
<% end %>